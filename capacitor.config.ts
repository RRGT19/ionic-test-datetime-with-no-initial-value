import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-test-datetime',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
